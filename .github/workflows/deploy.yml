name: CD Deploy

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy latest main branch via SSH
        run: |
          ssh -o StrictHostKeyChecking=no -p ${{ secrets.SSH_PORT }} ${{ secrets.USER }}@${{ secrets.HOST }} "
            APP_DIR=/home/${{ secrets.USER }}/mysite/site
            REPO_URL=https://github.com/darktezi/static-website-example.git
            BRANCH=main

            if [ ! -d \$APP_DIR ]; then
              echo '🚀 Клонируем репозиторий main...'
              git clone -b \$BRANCH \$REPO_URL \$APP_DIR
            else
              echo '🔄 Обновляем ветку main...'
              git -C \$APP_DIR fetch origin \$BRANCH
              git -C \$APP_DIR checkout \$BRANCH
              git -C \$APP_DIR pull origin \$BRANCH
            fi

            echo '📦 Текущая ветка:'
            git -C \$APP_DIR rev-parse --abbrev-ref HEAD
            echo '🧾 Последний коммит:'
            git -C \$APP_DIR log -1 --oneline

            echo '✅ Сайт обновлён в '\$APP_DIR
          "
