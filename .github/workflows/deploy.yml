name: CD Deploy

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy Docker container via SSH
        run: |
          ssh -o StrictHostKeyChecking=no -p ${{ secrets.SSH_PORT }} ${{ secrets.USER }}@${{ secrets.HOST }} "
            # 1. Логинимся в GHCR
            echo '🔐 Logging into GHCR...'
            echo '${{ secrets.TOKEN }}' | docker login ghcr.io -u '${{ github.actor }}' --password-stdin
            
            # 2. Скачиваем образ
            echo '📥 Pulling Docker image...'
            docker pull ghcr.io/darktezi/static-website-example:latest
            
            # 3. Останавливаем старый контейнер
            echo '🛑 Stopping old container...'
            docker stop static-site || true
            docker rm static-site || true
            
            # 4. Запускаем новый контейнер
            echo '🚀 Starting new container...'
            docker run -d \
              --name static-site \
              -p 80:80 \
              --restart unless-stopped \
              ghcr.io/darktezi/static-website-example:latest
            
            # 5. Проверяем
            echo '✅ Checking deployment...'
            sleep 5
            docker ps
            echo '📱 Testing site...'
            curl -s http://localhost | head -3 || echo '⚠️ Site not responding yet'
            
            echo '🎉 Deployment completed!'
            echo '🌐 Site available at: http://app.${{ secrets.ID }}.${{ secrets.PROXY_DOMAIN }}'
          "
