name: CD Deploy

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy Docker container via SSH
        run: |
          ssh -o StrictHostKeyChecking=no -p ${{ secrets.SSH_PORT }} ${{ secrets.USER }}@${{ secrets.HOST }} "
            # 1. Логинимся в GHCR для pull образа
            echo '${{ secrets.TOKEN }}' | docker login ghcr.io -u '${{ github.actor }}' --password-stdin
            
            # 2. Pull latest image from GHCR (собранный в CI)
            echo '📦 Pulling latest Docker image from GHCR...'
            docker pull ghcr.io/darktezi/static-website-example:latest
            
            # 3. Останавливаем и удаляем старый контейнер
            echo '🛑 Stopping old container...'
            docker stop static-site || true
            docker rm static-site || true
            
            # 4. Запускаем новый контейнер с настройкой перезапуска
            echo '🚀 Starting new container with restart policy...'
            docker run -d \
              --name static-site \
              -p 80:80 \
              --restart unless-stopped \  # Автоматический перезапуск
              ghcr.io/darktezi/static-website-example:latest
            
            # 5. Очистка старых образов
            echo '🧹 Cleaning up old images...'
            docker image prune -f
            
            # 6. Проверяем что контейнер запустился
            echo '✅ Checking container status...'
            docker ps | grep static-site
            
            echo '🎉 Deployment completed successfully!'
            echo '🌐 Your site is available at: http://app.akutin.course.prafdin.ru'
          "
